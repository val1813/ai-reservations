# LP29-C2T Re-escalation PI synthesis

Date: 2026-06-04

## Killed claims

1. Existing sources are sufficient to construct provenance-complete same-sample joined rows.

Killed by: all five current candidates remain `BLOCK`; none simultaneously carries same-sample crosswalk, graph convention lock, external label independence, forbidden-control audit, and applicable OI/Srivastava reproduction witnesses.

2. Round 3 blocker audit is sufficient as a closeout boundary product.

Killed by: REVIEWER Round 3. A blocker-status CSV without witness payload schema leaves `BLOCK/WARN/PASS` as reviewer-readable labels, not reproducible audit decisions.

3. Schema/source inventory alone makes `BLOCK/WARN/PASS` reproducible.

Killed by: Round 4. Schema conformance only proves shape; gate predicates and witness payloads are needed to derive admissibility.

## A/B convergence

A and B independently produced the same larger direction:

- A: external material validation rows are witness-executor decidable objects; without machine-reviewable same-sample, graph-lock, label-independence, forbidden-control, and baseline-reproduction witnesses, the validation object does not exist.
- B: the scientific unit is a proof-carrying row / temporal evidence DAG, not a flat joined-table row.

## PI re-escalated claim

**LP29-C2T-R1: In amorphous-material graph-vs-orbital-overlap validation, a candidate joined row is not a scientific validation unit until it carries a machine-checkable admissibility proof. `schema_conforms(row)` is not enough; `material_validation_allowed(row)` must be derived by an executor from witness payloads, gate predicates, temporal ordering, same-sample identity, label independence, graph protocol lock, forbidden-control audit, and applicable Srivastava/OI reproduction witnesses.**

Current five rows are the first negative examples:

- `C2T-AUDIT-0001` BLOCK
- `C2T-AUDIT-0002` BLOCK
- `C2T-AUDIT-0003` BLOCK
- `C2T-AUDIT-0004` BLOCK
- `C2T-AUDIT-0005` BLOCK

This does not validate material physics. It upgrades the problem from "assemble a table" to "define whether an external validation object exists."

## Registration decision

Register `LP29-C2T-R1` as a new candidate in the project queue during the queue-update step.

Provisional score estimate:

- impact breadth: 3 (methodology across LP29-style material validation)
- self-confirmation risk: 0.5
- completion bonus: 1.3
- claim fidelity: 1.0
- breakthrough potential: 1.0
- score: 1.95

Boundary:

- no material validation
- no Srivastava reproduction
- no graph-beats-overlap claim
- no same-sample closure claim
