# INSPECTOR Report: B C2 Round 1

Input: `D:\Claude\ai-reservations\LP29-AmorphousBands\current\B\c2_round1.json`

Scope: only B's C2 Round 1 derivation/protocol was inspected. A output was not read. Topic value and literature novelty are not judged here.

## Mechanical Check

`D:\Claude\ai-reservations\LP29-AmorphousBands\validation` does not exist, so `validate.py` / `quantum.py` mechanical checks are unavailable. Q1/Q4/Q5 were manually inspected.

Result: warning, not a blocker.

## Q1 Dimension Check

Status: pass with one convention warning.

Checked formulas:

- `S = sum w_ij`: unit is the same as the chosen overlap/edge-weight unit. Internally consistent if all `w_ij` are generated under one fixed convention.
- `L_w = D_w - A_w`: same unit as edge weights for an unnormalized weighted Laplacian; dimensionless for a normalized Laplacian.
- `lambda_2`: same unit as `w_ij` for unnormalized `L_w`; dimensionless for normalized `L_w`.
- `rho(A_w)`: same unit as `w_ij` for weighted adjacency unless weights are normalized.
- `damage_gap = D_target - D_random`: dimensionless if `D` is relative loss, as stated.
- `Y_perp = (I - P_B)Y_external`: same unit as `Y_external`.
- `M_perp = (I - P_B)M_graph`: same unit as `M_graph`.

Warning W1: `lambda_2` and `rho(A_w)` are listed as `dimensionless_or_weight_unit`. That is acceptable for a protocol, but the convention must be locked before any numerical comparison or regression, otherwise cross-run coefficients are not comparable.

## Q2 Direction Check

Status: pass.

Directional claims checked:

- Larger `lambda2_perp` predicts higher Hall mobility, Drude weight, Wannier bandwidth, and ridge coherence residuals. This matches the intended expansion/connectivity interpretation.
- Larger `lambda2_perp` predicts lower linewidth/IPR residuals. Direction is consistent with stronger delocalized backbone coherence.
- `damage_gap = D_target - D_random`; B predicts `D_target > D_random`. If `D` is a loss, then larger `damage_gap` should predict lower mobility/Drude/coherence labels. The synthetic label `Y = beta0*S_overlap + beta1*sigma_epsilon + beta2*lambda_2 - beta3*attack_gap + noise` has the correct sign for `beta3 > 0`.

No reversed ratio or eaten minus sign was found.

## Q3 Circular Argument Check

Status: pass with one protocol warning.

B explicitly forbids labels generated by the same graph Hamiltonian used to compute graph metrics, and separates external Hall/Drude/Wannier/spectral labels from synthetic diagnostic labels.

Warning W2: the `B_positive_label` synthetic benchmark is intentionally generated from `lambda_2` and `attack_gap`, so it can only validate software plumbing and null-control behavior. It cannot be cited as evidence for the material claim. The JSON states this guardrail correctly; next rounds must preserve the distinction.

## Q4 Order-of-Magnitude Check

Status: pass with one source warning.

No material-scale numerical prediction is made. The concrete numbers are benchmark design choices: `N=64..256`, synthetic controls, and pass/fail thresholds. There is no >10 or >50 order mismatch because no empirical magnitude is asserted.

Warning W3: pass/fail thresholds such as held-out improvement, sign stability, and finite-size requirements are protocol thresholds but are not sourced or justified in this C2 file. Treat them as predeclared heuristic criteria, not literature-derived constants.

## Q5 Algebra, Limits, and Numerical Sources

Status: pass with source warning.

Algebra/limits:

- Fixed `S=sum w_ij` does not fix `lambda_2`, `rho(A_w)`, or high-betweenness damage response. Counterexamples exist by rewiring weight placement at fixed total weight; the non-equivalence claim is algebraically sound.
- `P_B` residualization is valid as a projection operation if the baseline design matrix is fixed inside each training fold. It must not be fit once on train+test jointly.
- The limit `rewiring_at_fixed_S -> many_graphs` correctly supports non-equivalence between scalar overlap budget and global organization.
- If `D` is relative loss, `D_target-D_random` remains dimensionless and has a meaningful zero null.

Numerical source check:

- The claims section has empty `references`.
- C2 file uses named external label families and Srivastava-style overlap language, but it does not attach provenance inside the claim objects.

Warning W4: numerical/protocol choices and cited physics baselines need provenance flags before empirical use. This is not a blocker for Round 1 because the file is framed as a protocol, but it is insufficient for a later material-validation claim.

## Q6.3 Claim Shrinkage

Status: warning.

Previous C2 framing from gate material: after controlling orbital-overlap, onsite variance, density, mobility-edge margin, batch and finite size, graph spectral features still predict external residual labels.

Current B C2 Round 1 framing: no validated independent graph mechanism is claimed; the viable attack is a narrower falsifiable residual-prediction framework, with explicit downgrade if graph spectra fail residual tests.

Warning W5: claim scope and certainty shrink from a positive B proposition to a conditional protocol. This is scientifically cleaner, but it is still claim shrinkage under Q6.3. PI should recalculate the current C2 score and preserve the downgraded wording unless empirical external labels are added.

## Q6.4 Alternative Explanation

Status: pass with active-risk note.

B explicitly states the principal alternative explanation: graph metrics may be nonlinear reparameterizations of orbital-overlap geometry. It gives concrete exclusion tests: nuisance orthogonalization, degree/weight-preserving rewiring, leave-one-family-out validation, batch-preserving permutation, and external-label provenance.

Active risk: the alternative explanation is not empirically excluded in Round 1; it is only converted into the null model. That is acceptable for this protocol-stage output.

## Q6.5 Landing Calculation

Status: warning, not blocker.

B identifies the no-prior blank: whether graph spectral features add residual predictive power beyond a Srivastava-style orbital-overlap baseline.

Landing present:

- A concrete synthetic benchmark blueprint is specified.
- It includes graph families, matched overlap/degree/weight controls, synthetic positive/null/leakage labels, finite-size sweep, and pass/fail conditions.
- It specifies external material-label requirements and forbidden self-generated labels.

Warning W6: the C2 Round 1 output contains a blueprint but no executed C2 calculation, seed-level CSV, or joined material table. This satisfies the minimum "blueprint" landing level, but remains a half-finished protocol. Next B round should execute at least the synthetic A-null/B-positive benchmark or produce a concrete same-sample label table.

## Verdict

INSPECTOR result: pass with warnings.

Blockers: 0

Warnings: 6

The B C2 Round 1 file can be used as a protocol-stage artifact, but later reasoning must not treat it as empirical validation of independent graph physics.

--- 投喂下一轮 ---

必须修正（阻断级）:

None.

建议修正（警告级）:

1. Lock Laplacian/adjacency normalization before any numerical regression; otherwise `lambda_2` and `rho(A_w)` units/coefficient scales are not comparable.
2. Keep synthetic `B_positive_label` strictly diagnostic; it is generated from the tested graph features and cannot validate material claims.
3. Mark pass/fail thresholds and finite-size choices as heuristic unless sourced or justified.
4. Add provenance flags for Srivastava-style overlap baseline, external label definitions, and all numerical protocol choices before empirical use.
5. Preserve the downgraded conditional claim wording and ask PI to recalculate C2 score because the current claim is narrower than the gate proposition.
6. Execute the C2 synthetic A-null/B-positive benchmark or create a concrete same-sample external-label join table in the next B round.

---
