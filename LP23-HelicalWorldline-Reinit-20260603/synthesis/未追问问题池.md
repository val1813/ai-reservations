| K编号 | 结论一句话 | 未追问问题 | 类型 |
|---|---|---|---|
| K1 | 固定半径螺旋不生成光锥 | 若把“固定半径”放宽为参数随观测者或仿射参数变化，哪一步会首先破坏你这里的圆柱而不是光锥判定？ | 失效型 |
| K2 | 相位 Re/Im 不能直接当物理坐标 | 若强行把 Re/Im 赋予长度量纲，最小代价是引入额外度规、嵌入还是规范固定，它会在哪里与 Minkowski 结构冲突？ | 为什么型 |
| K3 | 可存活重表述是 null 方向标记而非光锥母线本体 | 这个“可存活版本”究竟是在重述标准对象，还是给出了一条此前没有的可检验关系？ | 反面型 |
| K4 | 下一卡点是区分量子相位与 celestial angle | 若只给 tetrad 而不给 momentum direction，区分 `phi` 与 `alpha` 的论证还是否成立？ | 推论型 |
| K5 | `theta=alpha` 不协变 | 是否存在任何受限观测者族，使 `theta=alpha` 仅作为局部规范选择而非物理等式保留下来？ | 跨域型 |
| K6 | 可用的是相位梯度不是相位值 | 如果只保留 `p_mu = hbar partial_mu theta`，那 LP23 剩下的“螺旋”成分到底还对应什么几何对象？ | 为什么型 |
| K7 | 整体 U(1) 相位不改 null direction | 若改用相对相位或双 spinor 结构，哪一个新自由度才真正进入方向判定？ | 推论型 |
| K8 | “螺旋”若存活只能转成 optical/contact/twistor 数据 | 在这三个方向里，哪一个最可能产生可失败的具体预测，而不是再次退化成术语改名？ | 反面型 |
| K9 | 标准光锥母线的 twist 为零 | 哪一种最小扰动会把这里的“坐标旋转效应”变成真正的 congruence twist，而不是仍旧坐标效应？ | 失效型 |
| K10 | twist 属于 congruence，不属于单条 generator | 如果只研究一簇极窄邻域射线，最小需要保留哪些横向导数信息才能让 twist 有定义？ | 为什么型 |
| K11 | U(1) holonomy 不蕴含 optical twist | 是否存在一个显式反例，能同时让 holonomy 非零而 optical twist 严格为零并保持所有假设不变？ | 反面型 |
| K12 | LP23 最小降级命题是“holonomy 影响可观测量而非直接生光锥” | 这个最小降级命题若为真，和现有 spin optics/Faraday-Berry 框架相比新增的判别量到底是什么？ | 推论型 |
| K13 | R1 强式 `F=chi omega` 当前证伪 | 哪一个具体障碍是独立致死点：bundle 不同、量纲不符、缺少自然 `chi`，还是缺少唯一 connection source？ | 为什么型 |
| K14 | R1 弱式只剩分层命题 | 如果只能分层共存而不能自然耦合，为什么这仍应算作 R1 的正向产出而不是失败后的整理？ | 反面型 |
| K15 | Round2 的最小检验口是“是否有严格新增量” | 这里的“新增量”判据是否预先固定过，可否被事后调低成“存在某种联系即可”？ | 失效型 |
| K16 | Round2 最小结构答案已被先发吸收 | 被先发覆盖的是对象、公式、还是可观测后果；覆盖边界在哪里结束？ | 为什么型 |
| K17 | 零 optical twist 与非零 internal holonomy 可共存但不新 | memory 支线为什么只能算旁证，缺的那一步是定量桥接、协变性，还是新颖性证明？ | 推论型 |
| K18 | Round3 生死检验是找 no-go 是否已被 prior art 覆盖 | 如果 no-go 只是在既有框架中的综合推论，它为什么足以结束 R1，而不足以生成一个更小的新候选？ | 反面型 |
| K19 | Round3 给出硬停止结论 | 去掉哪一个附加结构假设后，最有可能重新打开“自然耦合存在性”而不是直接回到旧框架？ | 跨域型 |
| K20 | R1 最终结论是强式证伪、弱式无新增量 | 若弱式无新增量，为什么当前北极星队列里仍保留该命题为当前北极星而非转入已收尾降级区？ | 推论型 |
| K32 | R4 Round1 显示开放闭包吸收而受限 toy 闭包保留非零类，但受限规则仍是人工 toy。 | 如果等价关系 `E` 或白化内积在有限截断后改变，K32 的非零 residual norm 是否仍代表同一个商类而不是内积选择产物？ | 失效型 |
| K33 | R4 Round2 的 toy witness checker 能拒绝 residual-shaped oracle，但尚未物理化 checker 的可信计算基。 | 是否存在不直接复制 residual、但由多列后验组合间接编码 residual 形状的 oracle 绕过 `W_origin/W_nonoracle` 单列检查？ | 反面型 |
| K34 | R4 Round3 判定新物理/新数学北极星硬停止，只能保留不输出 `NEW_PHYSICS` 的工具骨架 R4t。 | 在没有独立 holdout 的情形下，R4t 能否给出可复核的 false-positive/false-negative 边界，而不重新引入物理发现声张？ | 推论型 |
