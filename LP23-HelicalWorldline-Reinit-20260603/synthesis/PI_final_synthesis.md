# PI 最终综合推导

## 汇合判断

三轮 A/B 探索形成 **一致 + 互补**：

- **一致：** LP23 原始强声张不成立。固定半径单位螺旋不生成光锥；量子相位值 `theta` 不能直接等同 null 方向角 `alpha`；整体 `U(1)` 相位/holonomy 不自动产生 null congruence optical twist。
- **互补：** A 路线把失败定位到 Lorentz 几何、aberration、optical scalar；B 路线把失败定位到 fiber/base、projective spinor、contact/twistor holonomy。两条路线给出同一降级原则：fiber 变量不能直接充当 base 因果几何。

## 最终结论类型

**有边界 / 原始声张证伪 / 新北极星注册。**

原始 LP23-S1：

> 平坦时空中螺旋包络面严格等价光锥面 `ds^2=0`。

判定：**证伪。**

保留版本：

> 相位 holonomy 可影响干涉、偏振或 screen-frame 运输；null direction/光锥由 null vector、projective spinor、observer tetrad 或 twistor/contact lift 给出。若要声称统一，必须给出 spin/screen/contact connection 到 null congruence optical data 的量纲正确、协变耦合。

## 核心结论链

1. 固定半径单位螺旋或其相位平移族的自然集合是圆柱 `X^2+Y^2=ell^2`，不是光锥 `X^2+Y^2=c^2t^2`。
2. `e^{i theta}` 的 `Re/Im` 无量纲。要嵌入 Minkowski 几何，必须额外指定尺度、截面和坐标解释。
3. `theta=(Et-p·x)/hbar` 是 Lorentz 标量；null 方向角 `alpha` 是 observer celestial coordinate，boost 下 obeys aberration。因此 `theta=alpha` 不协变。
4. spinor bilinear `k^mu=xi^dagger sigma^mu xi` 在整体 `U(1)` 相位下不变；整体相位是 fiber/holonomy 变量，不是 null direction。
5. 标准光锥 congruence 是 hypersurface-orthogonal，optical twist 为零。单条 null generator 没有内禀 twist。
6. 非零 `U(1)` holonomy 可与零 optical twist 共存；Hopf/Berry 曲率非零不推出 spacetime screen twist 非零。

## Re-escalation 结果

新候选已注册为 `LP23-R1 Projective Spinor/Contact 联络统一`：

> 因果光锥、量子相位与“螺旋/扭转”的共同来源不是时空中的单相位轨迹，而是带联络的 projective spinor/contact 主丛；null cone 是商-投影，phase holonomy 是垂直曲率，optical data 是水平/screen 分布曲率。

当前优先级矩阵中 LP23-R1 与 LP23-S1 分数相同，未超过切换阈值 `当前×1.3`，所以不在本 Phase 中自动切换；LP23-S1 先完成收官。

## 结论边界

- 本 Phase 没有证明 LP23-R1 成立，只证明它是原始声张失败后更自然的可检验新北极星。
- 本 Phase 没有排除所有 spin/contact/twistor 统一可能性；只排除了无结构的“相位值/单位螺旋 = 光锥/扭转”。
- 后续若推进 LP23-R1，第一步必须检查 `F_AB=chi omega_AB` 或等价 screen/spin connection 耦合的量纲、协变性和自然性。

## 收官结论

LP23-S1 不能作为原声张继续推进。可收官为：

**有边界：原始几何直觉被证伪，但揭示出更深的 fiber/base 分离原则，并注册 LP23-R1 作为下一候选。**
