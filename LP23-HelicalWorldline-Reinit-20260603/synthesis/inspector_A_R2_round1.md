# INSPECTOR_A R2 Round1 校对

判定：警告，不阻断。

- `A'_lambda=A_lambda+dpsi/dlambda` 量纲通过；正号依赖本文 `SO(2)` 旋转约定。
- `Delta chi'=Delta chi+psi_f-psi_i` 量纲与方向通过。
- `H^{ab}(x)=1/2 chi^{abcd}(x)F_cd(x)` 量纲通过。
- `H^{ab}(x)=1/2 integral K^{abcd}(x,x')F_cd(x')d^4x'` 有条件通过：要求 `[K dV_{x'}]=[chi]`。
- 非局域核公式必须显式规定积分测度、delta 归一化与 `K` 的密度权重。

修正要求：固定 screen 旋转约定；将非局域积分写成协变体积测度 `dV_{x'}=sqrt(-g(x'))d^4x'`，并说明 `K dV` 与 `chi` 同量纲。
