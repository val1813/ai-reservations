# INSPECTOR_A R2 Round3 校对

判定：警告，不阻断。

主要警告：

1. `I_bridge = ||(I-Pi_standard)r||^2` 方向正确，单位为 `[r]^2`。
2. 白化正交残差应写为 `||(I-Pi_white) C^{-1/2} r||^2`，其中 `Pi_white` 是白化后标准子空间上的投影；不能直接把 `C^{-1/2}` 放在未加权投影外面。
3. “标准框架完全吸收/不再有独立地位”超出投影公式本身，应表述为“在给定标准联合子空间定义下，它是模型失配诊断量”；覆盖性依赖外部文献判断。

无阻断。
