# B博士 Re-escalation 草案

## 新声张草案（<=300字）

LP23 更大声张：因果光锥、量子相位与“螺旋/扭转”的共同来源不是时空中的单相位轨迹，而是带联络的 projective spinor/contact 主丛；null cone 是商-投影，phase holonomy 是垂直曲率，optical data 是水平分布曲率。X1-X4 被杀，是同一深层原则的推论：纤维变量不能直接充当基空间因果量；只有自然的 screen/spin connection 耦合才能把二者锁成可观测几何。

## 简短验证框架

- 最小数学对象：`C^2\{0}` 上的 `U(1)` 主丛、Pauli 映射 `k^mu=xi^dagger sigma^mu xi`、null screen/contact connection。
- 驱动矛盾：垂直 `U(1)` holonomy 与水平 null congruence twist 分属不同 bundle，却可能被同一联络耦合。
- 第一步计算：验证 `xi -> e^{i psi}xi` 下 `k^mu` 不变；再在光锥 congruence 上算 `F_H=(1/2)sin chi dchi wedge dphi` 非零而 `k wedge dk=0`，定位所需耦合项。
