# PI审核 Phase 2 — LP1-S6_LiebRobinsonBound

## 审核结论

`blocked` 的 LR 重定义成立。

## 核心判断

- 改变的是阈值常数，不是渗流结构。
- `p_block(W)` 仍然可作为相变变量。

## K条目

- **K1.2**: `site j is blocked` 可由 `d(j,∂H) > v_LR tau_th` 重定义。
- **K1.3**: 新判据只改变常数，不改变 `p_block(W)` 的临界结构。

## 下一步

▶️ 下一步：执行Phase 3，证明新判据下的 `p_block(W)` 与原渗流框架一致

