# 研究计划 — LP1-S6_LiebRobinsonBound v1

**课题：** LP1-补11 Lieb-Robinson界  
**日期：** 2026-06-01

---

## 驱动矛盾

- 命题A：`blocked` 判据可以由 `tau_th < tau_local` 启发式定义。
- 命题B：论文级表述要求显式传播速度上界 `v_LR(W,g)`，否则 `blocked` 没有严格意义。

---

## Phase 1

**目标：** 写出 2D extended Hubbard 模型的 Lieb-Robinson 上界结构。

**A任务：**
- 调用标准 LR 定理
- 把局域相互作用常数、耦合半径、算符范数统一到 `v_LR(W,g)` 的上界表达里

**B任务：**
- 独立检查最适合放进论文的判据形式

---

## Phase 2

**目标：** 用 `v_LR` 重定义 `site j is blocked`。

---

## Phase 3

**目标：** 证明新判据下的 `p_block(W)` 与原渗流框架一致。

