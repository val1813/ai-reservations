# Current State: LP28-CphiPacket

Project: LP28-NickelateLayerDecoupling
Active north star: LP28-CphiPacket

## North Star

Can a real same-sample or matched-specimen nickelate packet supply `P_oe + B_min` before superconducting-output reveal, strongly enough to kill or preserve `R_oe`?

## Why This Is Not A Downgrade

`LP28-CphiCollapse` ended as bounded protocol. REVIEWER correctly blocked any claim of theory breakthrough without data.

The next real bottleneck is not paper framing or formula tuning. It is whether the required metrology packet can exist:

- odd/even bilayer susceptibility for `P_oe`;
- measured non-tautological `B_min`;
- independent calibration and denominator floor;
- no superconducting-output leakage;
- enough same-sample or matched rows for cross-fitted residual testing.

If the packet cannot exist, `C_phi/R_oe` is practically dead. If it can exist, then the residual-collapse protocol becomes executable.

## Core Contradiction

Proposition A:

A usable packet can be assembled from current or near-future nickelate measurements by aligning qz/polarization/form-factor spectroscopy, oxygen/structure/orbital/disorder/c-axis baselines, and sealed output labels.

Proposition B:

The packet is not physically/experimentally separable. The probes needed for `P_oe` and `B_min` share sample history, calibration, qz response, pressure path, and matrix-element corrections so strongly that `R_oe` cannot be identified without leakage or tautology.

Both cannot be true in the same research window.

## Immediate Status

Workspace created: `current-CphiPacket/`.

Initial search:

- `D:\文献` filename scan found no explicit La3Ni2O7 / nickelate / RIXS / Drude file hits.
- paper-search-mcp found adjacent sources on structural evolution, pressure phase transitions, bilayer magnetic excitations, RIXS, optical correlations, and oxygen/ligand-hole baselines.
- No complete same-sample `P_oe + B_min` packet was found at startup.

## Current Allowed Claim

Only:

`LP28-CphiPacket asks whether the data packet required by the bounded R_oe protocol is feasible.`

Not allowed:

- `R_oe` exists.
- `C_phi` is independent.
- current literature already validates the packet.

## Round 1 Result

Round 1 completed.

A verdict:

`PACKET_FEASIBLE_NEAR_FUTURE`

B verdict:

`PACKET_NONIDENTIFIABLE_FROM_CURRENT_LITERATURE`

PI synthesis:

No complete existing leakage-clean same-sample or blinded matched-specimen `P_oe + B_min` packet was found.

The real conflict is now:

- Can a new sealed packet be engineered with enough row identity, calibration, denominator, `U_miss`, batch, and output-blinding controls?
- Or are `P_oe` and `B_min` too coupled through qz/c-axis response, pressure path, oxygen, structure, orbital state, matrix elements, and sample quality to be independently identified?

Inspector warnings carried forward:

- `P_oe` definition drift must be fixed. The canonical definition remains `(chi_odd-chi_even)/(chi_odd+chi_even)` with censoring/floor rules. Any L1-stabilized denominator must be renamed and not mixed with prior `P_oe`.
- `B_min` independence from `P_oe` extraction is not proven by separate scripts alone.
- B's falsifier threshold must be quantified beyond "several independent rows".
- Research Square records and ACS SI DOI are weak anchors unless parent/final records are verified.

Breakthrough status:

Not a breakthrough yet. Round 1 blocks retrospective literature montage and forces the next step into row-level sealed packet design or structural non-identifiability.

Next:

Round 2 must quantify the packet: row schema, minimum N, batches, probe order, independence rules, and falsifier thresholds.

## Round 2 Result

Round 2 completed.

A verdict:

`PACKET_FEASIBLE_NEAR_FUTURE_WITH_HARD_SCHEMA`

B verdict:

`PACKET_NONIDENTIFIABLE_UNLESS_MINIMUM_VALID_DESIGN_IS_MET`

Unified PI gate:

- `N=12`: rehearsal only.
- `N=24`, `>=18` valid uncensored `P_oe` rows, `>=2` batches: minimum executable protocol only.
- `N=48`, `>=36` complete rows, `>=4` batches, `>=3` labs, leave-one-batch/lab-out validation: decision-grade / B-concession target.

These are protocol gates, not statistical power theorems.

Important fixes:

- Canonical `P_oe` is restored as `(chi_odd-chi_even)/(chi_odd+chi_even)`.
- L1-stabilized denominator is renamed `P_oe_L1_diagnostic` and cannot drive the primary residual call.
- The main unresolved risk is qz/c-axis/matrix-element same-source pollution between `P_oe` and `B_min`.
- Sibling rows need pre-output mismatch tolerances; they cannot be rescued by post-hoc comparison to observed residuals.

Round 3 target:

Produce the final green/yellow/red row firewall and kill table:

- `NOT_FOUND`
- `NON_DECISION_GRADE`
- `NON_IDENTIFIABLE`
- `PROTOCOL_FAILURE`
- `PACKET_FEASIBLE_NEAR_FUTURE`

## Round 3 Reviewer Result

REVIEWER verdict:

`REJECT`

Core reason:

The packet is still a disciplined plan, not a claim-bearing packet. It has no audited real pilot row.

PI accepts the rejection as a valid fatal attack on the current state.

This does not prove universal impossibility. It means `PACKET_FEASIBLE_NEAR_FUTURE` is too speculative unless a real pilot row or formally sistered row can be specified.

Forced salvage target:

one audited pilot row demonstrating:

- immutable raw archive;
- independent `P_oe` / `B_min` extraction;
- frozen canonical `P_oe` censoring;
- frozen `U_miss`;
- sealed output labels;
- pre-reveal green/yellow/red firewall.

If this cannot be made concrete, LP28-CphiPacket closes as rejected protocol packaging.

## Forced Salvage Close

Forced salvage completed.

Salvage 1:

- defined the minimum real object: one green `PILOT-001` audited pilot row.
- conclusion: route text alone does not overturn REVIEWER; real green row is required.

Salvage 2:

- A identified a real contactable route: Diamond Light Source I21 RIXS plus Diamond raw-data archive / ICAT-Topcat style custody path.
- B held that a template is not enough; without a real row, salvage success is not achieved.
- PI independent verification accepted the route as real/contactable, not as completed data.

Final status:

`CONDITIONALLY_KEEP_AS_CONTACTABLE_DOSSIER`

Allowed:

- use `synthesis/PILOT-001_dossier_template.md` for external acquisition planning;
- pursue Diamond I21 or equivalent facility contact;
- reopen only when a real archive/custody route or data source is available.

Forbidden:

- more internal protocol elaboration without new external facility/data input;
- claiming `R_oe`;
- claiming packet feasibility beyond a contactable route;
- claiming REVIEWER rejection was fully overturned.

Practical next artifact:

`current-CphiPacket/synthesis/PILOT-001_dossier_template.md`
