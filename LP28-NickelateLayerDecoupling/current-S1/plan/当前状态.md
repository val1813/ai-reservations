# Current State: LP28-S1

Project: LP28-NickelateLayerDecoupling
Active window: LP28-S1
Contributor: {"orcid":"anonymous","github":"anonymous","name":"anonymous contributor"}

## North Star

Can `I_phi` be pre-registered from input-side spectroscopy / structure / dephasing variables independently of bulk superconducting output, or is it merely a posterior relabeling of the bulk phase boundary?

## GATE -1 Core Contradiction

Proposition A: `I_phi` can be defined before looking at output variables, using only input-side measurements such as interlayer `dz2` spectral weight, c-axis optical response, structural apical-O geometry, and an independently measured dephasing scale.

Proposition B: any useful `I_phi` is necessarily fitted after observing `Tc`, shielding, zero resistance, or bulk phase stiffness, so it is not a predictor but a relabeled outcome.

Logical conflict: A and B cannot both be true for the same protocol. If construction or thresholding of `I_phi` uses bulk superconducting output, S1 fails as a pre-registration protocol even if the index correlates with superconductivity.

## Boundary Rules

- Do not claim `I_phi` is validated.
- Do not use `Tc`, zero resistance, Meissner fraction, shielding, critical current, or Josephson-like bulk phase stiffness to construct or tune `I_phi`.
- Allowed input-side variables must be measurable on the same sample before declaring the bulk output.
- The target is a non-circular experimental protocol, not a paper claim.

## Round Counter

Round: closed after 3
Status: LP28-S1 closed as a bounded preregistered protocol template, not as a scientific claim. REVIEWER rejects predictor/mechanism language and allows only major revision as protocol.

## Round 1 Summary

- A: `I_phi` can only survive as a narrow pre-registration protocol; `C_phi` is the main circularity bottleneck.
- B: the predictor/outcome split can be formalized as zero-leakage coding plus input-side network observability.
- INSPECTOR: no blocker; warnings require exact units, divergence control, frozen windows/rules, and mock same-sample calculation.
- R1 prior-art WebSearch: physical mechanism space is already heavily covered by interlayer-coherence and `dz2-pz-dz2` prior art; novelty must remain methodological.

## Next Step

Next priority should not tune `I_phi` algebra. Reopen only with a real sealed P0 feasibility test or a focused baseline-collapse attack.

## Known Prior-Art Boundaries

- Interlayer electronic coherence as an organizing parameter in RP nickelates is prior art.
- `dz2-pz-dz2` hybridization enabling superconductivity is prior art.
- DW disappearance alone is not sufficient for bulk superconductivity.
- LP28-S1 novelty, if any, must live in the non-circular pre-registration protocol and the separation of input-side phase-bus readiness from bulk output.
