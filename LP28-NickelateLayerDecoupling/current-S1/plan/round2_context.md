# Round 2 Context For A/B

Round 1 surviving claim: `I_phi` is not validated and not a mechanism. It survives only as a frozen input-side encoder computed before bulk superconducting output is inspected.

Strongest convergence: `C_phi` is the bottleneck. If it uses Josephson plasma, superfluid density, Meissner screening, zero resistance, critical current, bulk phase stiffness, or transition-width fits, S1 fails by output leakage.

Round 2 target: turn `C_phi` into an executable input-side observability audit. Define units, windows, coefficients, missing-data rules, divergence control, and a three-sample mock preregistration packet. Include baseline-collapse checks against single-variable alternatives.

