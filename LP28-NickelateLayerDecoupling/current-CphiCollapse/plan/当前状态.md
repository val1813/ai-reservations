# Current State: LP28-CphiCollapse

Project: LP28-NickelateLayerDecoupling
Active north star: LP28-CphiCollapse

## New North Star

Does a genuinely independent input-side `C_phi` exist for nickelate phase-bus readiness, or does every experimentally measurable `C_phi` inevitably collapse to prior-art c-axis coherence, disorder, oxygen stoichiometry, or sample quality?

## Why This Is The Real Contradiction

Reviewer did not merely "downgrade" S1. Reviewer exposed the buried contradiction:

- Proposition A: `C_phi` is an independent input-side variable measuring recoverable interlayer phase-bus readiness before bulk superconducting output.
- Proposition B: every measurable `C_phi` is a proxy for already-known c-axis coherence / sample quality / oxygen stoichiometry / pressure phase context, so it cannot carry new theoretical content.

Both cannot be true. If B is true, `I_phi` is dead regardless of better protocol engineering. If A is true, then nickelate screening should shift from `Tc` and zero resistance to a new input-side phase-bus variable.

## AHA Source

AHA #S1-1: `C_phi` is not primarily "how strong the coherence is"; it is the bottleneck determining whether `I_phi` is a zero-leakage pre-registered input-side variable or an output-contaminated relabeling.

## Claim Boundary

This is not for writing a paper. The goal is to decide whether a real theoretical/technical variable exists.

Allowed:

- attack `C_phi` until it either becomes nontrivial or dies
- use baseline-collapse as a killing test
- look for an invariant or no-go theorem

Forbidden:

- beautifying the preregistration protocol
- claiming predictor validation
- treating c-axis Drude fraction as novelty
- optimizing formula coefficients

## Round 1 Result

Round 2 completed.

Core hinge:

`eta` in `X_pre = g(B, eta)` is unresolved.

A interprets `eta` as a possible odd/even bilayer phase-transfer residual:

`P_oe = (chi_odd - chi_even)/(chi_odd + chi_even)`

B interprets `eta` as missing baseline confounding: oxygen, strain, dephasing, dz2/self-doping, pressure history, and sample quality.

Round 2 stabilized the hinge:

`R_oe = P_oe - E[P_oe | B_min]`

A says `R_oe` can survive as a measurable bilayer parity-transfer residual.

B says `R_oe` will vanish once `B_min` includes measured oxygen/layer asymmetry, strain, dz2/RE5d/self-doping, dephasing, pressure path, and c-axis coherence.

Round 3 must specify exact go/no-go thresholds and gray-zone handling.

## Immediate Round 2 Target

Find a discriminating criterion:

What observation would separate "independent phase-bus readiness" from "ordinary c-axis coherence/sample quality" without using superconducting output?

## Round 3 Result

Round 3 completed with mandatory REVIEWER.

Current status:

- `A/round3.json` passed INSPECTOR with warnings.
- `B/round3.json` initially hit a blocker: signed pair residual direction was not preregistered and "same-sign abs(Delta_r_pair)" was invalid.
- `B/round3_revised.json` fixed the blocker by making the primary paired test direction-free and allowing signed tests only under a pre-frozen `P_oe/R_oe`-blind orientation axis.
- `synthesis/inspector_B_round3_recheck.md` passed the revised B protocol.
- `synthesis/pi_round3_synthesis.md` defined the killable object:

`R_oe = P_oe - E_crossfit[P_oe | B_min]`

where

`P_oe = (chi_odd - chi_even)/(chi_odd + chi_even)`.

REVIEWER verdict:

`PASS AS BOUNDED PROTOCOL`.

Reviewer explicitly rejected treating this as a theory breakthrough candidate. The current valid claim is narrower:

`LP28-CphiCollapse` has produced a strict residual-collapse protocol that can kill or preserve an independent `C_phi` object if a real same-sample packet exists. It has not shown that `C_phi` exists, and it has not shown universal collapse.

## Round 3 Breakthrough Check

Not a breakthrough yet.

The advance is structural: the vague `C_phi` claim has been forced into a falsifiable residual object, `R_oe`, with leakage, denominator, calibration, small-N, `U_miss`, batch, sample-quality, and probe-calibration gates.

This is closer to actual technology progress than formula tuning because it prevents wasted screening on relabeled c-axis coherence. But it remains bounded until a same-sample pressure/matched-specimen packet is acquired.

## Round 3 AHA Check

No new stronger AHA was registered.

Round 1 AHA remains stabilized:

the true object is `eta/R_oe`, not scalar `C_phi`.

Round 3 adds an execution correction, not a new north star:

residual survival cannot depend on post-hoc signed pair direction. It must survive direction-free paired magnitude tests or a signed test with an independently frozen orientation axis.

## Next Gate

N = 3 reached. Enter closeout gates, but preserve the REVIEWER boundary:

- do not claim breakthrough;
- do not claim `R_oe` survival;
- do not claim `C_phi` collapse;
- treat the output as a bounded protocol awaiting real same-sample data.
