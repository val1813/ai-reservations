# PI Final Synthesis: LP28-CphiCollapse

## Final Decision

`LP28-CphiCollapse` closes as:

**bounded protocol / no empirical decision yet**

It is not a theory breakthrough candidate yet. It is also not dead.

The useful result is that the independent `C_phi` claim has been forced into one killable object:

`R_oe = P_oe - E_crossfit[P_oe | B_min]`

where:

`P_oe = (chi_odd - chi_even)/(chi_odd + chi_even)`

## What Was Actually Learned

The original scalar `C_phi` was too easy to relabel as c-axis coherence, sample quality, oxygen disorder, strain, dephasing, orbital self-doping, or pressure path.

The only non-degenerate version left is:

an input-side odd/even bilayer parity-transfer residual that survives after measured, non-tautological `B_min` is removed.

That survival must be tested without superconducting-output leakage, without post-hoc signed pair direction, without denominator artifacts, and without allowing `U_miss` to absorb residuals after the fact.

## Boundary Accepted From REVIEWER

REVIEWER is right:

Current work is not a new physical law, not a validated variable, and not a proof of collapse. It is a strict residual-collapse decision protocol.

Allowed final claim:

`LP28-CphiCollapse defines a bounded, leakage-controlled test for whether an independent C_phi-like bilayer parity-transfer residual exists in a same-sample nickelate packet.`

Forbidden final claims:

- `R_oe` exists.
- `C_phi` is independent.
- all `C_phi` definitions universally collapse.
- `P_oe` is novel as an odd/even bilayer observable.
- this is already a phase-bus theory breakthrough.

## Minimum Experiment/Data Packet

The next real step is not more algebra. It is a feasibility/data-packet north star:

Can a same-sample or matched-specimen packet provide:

- qz/polarization/form-factor odd/even susceptibility for `chi_odd`, `chi_even`;
- full measured `B_min` before superconducting-output reveal;
- independent `S_channel -> chi_unit` calibration;
- frozen denominator reference and censoring;
- frozen `U_miss` envelope;
- batch, sample-quality, probe-calibration, non-bilayer, high-disorder, and probe-window controls?

Without this packet, the result stays bounded protocol.

## AHA Status

Registered/stabilized AHA:

`C_phi` is not the object. `eta/R_oe` is the object.

Round 3 added a methodological hardening:

survival cannot depend on post-hoc residual sign direction. It must pass direction-free paired magnitude tests or a signed test whose axis was frozen before `P_oe/R_oe` extraction.

This is not a new north star by itself; it is a guardrail for the next one.

## Closeout Verdict

Close `LP28-CphiCollapse` as bounded and unresolved:

- protocol: preserved;
- independent `C_phi`: unproven;
- collapse/no-go: unproven;
- next required north star: real packet feasibility and execution, not formula tuning.
