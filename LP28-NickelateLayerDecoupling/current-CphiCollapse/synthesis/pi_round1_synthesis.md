# PI Synthesis: LP28-CphiCollapse Round 1

## Inputs

- A Round 1: `current-CphiCollapse/A/round1.json`
- B Round 1: `current-CphiCollapse/B/round1.json`
- INSPECTOR A Round 1: warning, not blocker
- INSPECTOR B Round 1: warning, not blocker
- R1 paper-search boundary check

## Core Result

This round found the real hinge.

A's best candidate for independent `C_phi` is not c-axis Drude weight, not total c-axis spectral weight, not `dz2` weight, and not dephasing. It is:

`P_oe = (chi_odd - chi_even)/(chi_odd + chi_even)`

then residualized against:

`W_c, f_D, gamma, V_O, epsilon_strain, Z_dz2`

So the candidate independent variable is:

`C_phi_ind = residual(P_oe | W_c, f_D, gamma, V_O, epsilon_strain, Z_dz2)`

B's no-go attempt is strong but not closed. It argues all allowed inputs are generated by the baseline vector `B`, so `I(C_phi; Z | B)=0`. INSPECTOR caught the gap: if `X_pre = g(B, eta)`, B has not classified or eliminated `eta`. That `eta` is exactly where A's residual odd/even channel might live.

## New AHA Candidate

The theoretical object is no longer `C_phi` as a scalar coherence score. It is the status of `eta`:

- A: `eta` contains an odd/even bilayer phase-transfer residual `P_oe` that is input-side measurable.
- B: `eta` is only unmeasured oxygen/strain/dephasing/orbital/sample-quality noise.

This is a sharper contradiction than S1.

## INSPECTOR Warnings

A must fix:

- define `P_oe` sign, singular limits, and leakage-free measurement protocol
- state whether same-sample data exist
- test whether oxygen/strain/dephasing covariance annihilates the residual
- downgrade "minimal non-collapsing object" to candidate until measured

B must fix:

- classify or eliminate `eta`
- state assumptions behind `I(C_phi;Z|B)=0`
- make Kubo reduction complete or explicitly schematic
- narrow universal no-go to empirical non-identifiability if proof is unavailable

## Breakthrough-Direction Check

This is closer to a real theoretical breakthrough than the preregistration protocol. If A survives, the control variable is an odd/even bilayer phase-transfer residual, not generic interlayer coherence. If B survives, we get a no-go: nickelate phase-bus predictors cannot be independent without a genuinely new input-side observable.

Either outcome matters.

## Round 2 Task

Round 2 must attack `eta/P_oe` directly:

1. A: give a leakage-free measurement route for `chi_odd` and `chi_even`, ideally using neutron/RIXS/Raman/THz/c-axis optical combinations, with signs and singular limits.
2. B: prove `eta` is just missing baseline structure, or specify the minimal additional baseline that annihilates A's residual.
3. Both: provide a concrete matched-pair or intervention test where A and B predict opposite pre-output ranks.

## Stop Condition

No hard stop. N=1<3, continue Round 2.

