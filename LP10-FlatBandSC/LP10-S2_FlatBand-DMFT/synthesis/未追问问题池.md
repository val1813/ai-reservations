# 未追问问题池 — FlatBand-DMFT (LP10-S2)

> 由 AUDITOR（逻辑审计员）填充。每行：触发该问题的结论 | 未被追问的问题 | 类型。供 PI / Phase2 取用。

| 结论 | 问题 | 类型 |
|------|------|------|
| A 步4：Khurana 定理 ⇒ 单点DMFT横向/几何顶点修正=0 | Khurana 原证依赖单带 v_k 的 k-奇偶相消；多带几何超流权重是带间（interband）量，矩阵元非 parity-odd。Khurana 相消论证是否真的覆盖带间几何顶点？还是 A 把单带纵向电导结果越界搬到多带带间量？ | 适用域越界 |
| A 步4：δΓ_μ ∝ ∂Σ/∂k=0 故顶点修正恒为零 | ∂Σ/∂k=0 是 Ward 恒等式对*纵向*顶点的约束；横向/几何响应不被 Ward 钉死。用纵向 Ward 恒等式断言横向几何顶点为零，逻辑是否成立？ | 逻辑/Ward 恒等式误用 |
| A「漏几何」推卸到 (C3)失效区 / Phase3 校准 | 在 (C1)-(C4) 满足的孤立平带区内，单点DMFT漏掉的非局域两粒子顶点修正对 D_s^geom 的量级是多少（O(1) 还是次主导）？这是核心裁决，A/B 均未计算，能否用对称论证或单一可解模型（pyrochlore+SOC）定量定出？ | 计算缺口/核心裁决 |
| B「漏项 O(1)，因平带 W/U→0 无小参数」 | O(1) 仅是标度断言，B 未给系数。能否给出至少一个模型的顶点修正/裸气泡比值的数值或闭式，把「O(1)」从断言升为证据？ | 标度断言未量化 |
| A/B 同指「精炼B2」但 A 说不丢、B 说丢 | A 的「不丢」与 B 的「丢」谈的是同一两粒子顶点量、结论相反。除「常规带内 parity-odd 扇区一致」外，二者在带间几何扇区的矛盾如何用一个判别实验/可解模型裁决？ | A/B 真冲突裁决 |
| B N.2：存在 U_c 使 N₃[G]≠裸C，下界标度 π\|N₃\| 在 U_c 不连续 | A 假设 (C2) 能隙开则 C 不变（Wang-Zhang h_t=h+Σ(0)）；B 指出 G-零点（Mott邻近）可不闭单粒子能隙而使 N₃ 跳变。(C2) 是否足以排除 G-零点型拓扑相变？「能隙开」与「G(0)无零点」是否等价？ | 拓扑稳定性/(C2)充分性 |
| A c'=cZ·min_k w_norm，承认可 →0（撞墙2/3） | 一个被允许退化为「≥0」的下界在数学上恒真但无信息。在何种 (C1)-(C4) 子区间能保证 min_k w_norm 有正下界、Z 不趋零，使「存活」非平庸？需明确该非平庸子区间的物理边界 | 平庸化/非空性 |
| A/B 均不独立校验 S1 的 K1.1/K1.3/反例D | S2 全部 ✅ 悬挂于 S1 三条内部、无外部出处的前提。S1 的 K1.1（平均场 √det D_s≥cΔπ\|C\|）本身是否已通过 S1 阶段的 REVIEWER/AUDITOR？S2 收官前是否需回检 S1 状态？ | 继承性依赖/跨阶段 |
| B N.3：有限 T 下 \|C\|→混合态 C(T) 去量子化（热 Uhlmann-Chern） | A 框架把有限 T 仅吸收进重整 Δ(T)，未处理 Chern 数混合态去量子化。D_s(T) 的下界究竟是「T=0 整数 C 钉住的渐近+热软化」还是严格整数下界？两种解读对 BKT（Phase3）约束有何不同？ | 混合态/有限T 含义 |
| 特别审计：判「已被文献解决」需 REVIEWER 查重 | 本案 REVIEWER 记录仍空缺。2404.12993 是否已显式写出 π\|C\| 拓扑标度形式的*严格下界*（而非「good indicator」措辞）？2506.18969 的 bootstrap 下界与 DMFT π\|C\| 下界是否同一对象？需 REVIEWER 实读核实后方可定先发性质 | 先发查重/程序前置 |
