# 文献库

## Round 1 检索

### GUP / Lindblad 先发

- Petruzziello and Illuminati, 2021, `Quantum gravitational decoherence from fluctuating minimal length and deformation parameter at the Planck scale`, Nature Communications. DOI: `10.1038/s41467-021-24711-7`.
  - 相关性：fluctuating minimal length / deformation parameter -> Lindblad master equation 是强先发模板。
  - 对 DGF-N4 的约束：DGF 不能声称 GUP-Lindblad 链全新；新意只能在 `q` 同时控制 `m(q)` 与 `beta(t)`。

### 微克猫态 / 实验数据

- Fadel, 2023, `Probing gravity-related decoherence with a 16 microgram Schrodinger cat state`, arXiv:`2305.04780`.
  - 相关性：16 microgram 正好高于 DGF-N4 的 `13.85 microgram`，是优先重分析对象。

### 先发状态

- paper-search-mcp 未命中 exact `2m_p/pi` / `14 microgram` 质量上界直接先发。
- 该结论只能写成“不完整检索未命中”，不能写成“无先发”。

## 反例栏

- 若 `q` 只能由质量反推，则 DGF-N4 是质量重参数化。
- 若 `q_inv` 非实无法映射为可观测异常，则 `14 microgram` 只是定义域边界，不是物理边界。
- 常规环境退相干、DP、CSL、热噪声、振动、散射和制备效率均可解释质量相关 visibility 下降，必须作为替代解释。
