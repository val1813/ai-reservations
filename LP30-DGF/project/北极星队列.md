# LP30-DGF 北极星队列

## 当前北极星

当前北极星：DGF-N5 | 分数：3.75 | 影响：基础原理 | 风险：中 | 完成度：0%

| ID | 命题 | 影响宽度 | 自毁风险 | 完成度 | 声张保真度 | 突破潜力 | 分数 | 状态 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| DGF-N5 | DGF 的更深声张是跨表征不变量/不可辨识性原理：若端点结构在冻结逐点 `q_i` 后仍跨样本保持，则它比质量上界机制更基础；若消失，则机制声张终止。 | 5 | 0.5 | 1.0 | 1.0 | 1.5 | 3.75 | 当前：由 DGF-N4 强制挽救失败后切入，待 AB 验证 |

## 降级队列

| ID | 命题 | 分数 | 降级原因 | 断点文件 |
|---|---|---:|---|---|
| DGF-N0 | 大质量极限不是单纯的量子极限，而对应一种可观测的空间信息损失。 | 1.00 | GATE -1失败：宏观经典性与幺正量子演化不构成逻辑矛盾。 | `synthesis/GATE_minus_1.md` |
| DGF-N4 | 独立干涉坐标 `q∈[0,1]` 控制质量映射 `m(q)=(2m_p/pi)sin(pi q/2)` 并给出 `2m_p/pi≈13.85 microgram` 相干质量上界。 | 1.02 | 证伪-降级：Round 3 REVIEWER 致命阻断；强制挽救轮仍未证明外部 `q_DGF_mass` observable，且低秩/图模型自由度与 penalty 未闭合。按 `max(1,3.41*0.3)=1.02` 降分。 | `synthesis/REVIEWER_round3.md`; `synthesis/INSPECTOR_rescue.md` |

## 新候选（待AB验证）

| ID | 命题 | 分数 | 来源 | 验证状态 |
|---|---|---:|---|---|
| DGF-N4a | `I=(m/m_p)^2` 只能作为低质量校准；全域变量必须是独立干涉坐标 `q`。 | 3.00 | 大I极限修正 | 待AB |
| DGF-N4b | GUP-Lindblad 退相干核若要属于 DGF，必须证明同一个 `q` 的涨落同时驱动 `m(q)` 与 `beta(t)`。 | 2.25 | GUP链统一 | 待AB |
| DGF-N4c | 粒子质量谱不是 DGF 当前版本的输出，而是 `Q|i>=q_i|i>` 的外部本征值问题。 | 2.25 | 质量谱未结事项 | 待AB |
| DGF-N3 | 16-22 microgram 机械猫态实验可直接检验或排除 `14 microgram` vs `22 microgram` 边界。 | 1.50 | 实验靶点 | 待AB |
| DGF-N4d | Fisher-Rao/Hellinger 角可能给出 `q` 的独立信息几何定义，但必须找到实验 readout。 | 3.00 | B Round 1 AHA | 待AB Round 2 |
| DGF-N4e | `2m_p/pi` 是 `q_visibility=q_DGF_mass` 约束统计模型的可拒绝边界，而非自然禁止质量超过边界。 | 3.00 | Round 2 A/B汇合AHA | 待AB Round 3 |
| DGF-N4f | 用 Fadel/Bild 16 microgram 数据执行 M0/M1 likelihood 审判；若 M1 需要逐点 q_i 才拟合，则 DGF-N4 降级为经验模型。 | 3.50 | Round 3 AHA | 待REVIEWER后决定 |
| DGF-N6 | 外部质量代理 `q_DGF_mass` 是否可在不使用 visibility 数据的情况下独立标定；若不可得，则所有 DGF 质量-可见度桥接退化为内部一致性拟合。 | 3.75 | 降级条件B-知识空白 | 待AB |
| DGF-N7 | 在预注册图、秩、正则强度和特征字典后，低秩/图拉普拉斯 `q` 模型是否仍优于环境 null；若优势消失，则所谓端点结构只是后验灵活度。 | 3.00 | 降级条件B-副产物 | 待AB |
| DGF-N8 | fold-type critical endpoint 是否在 visibility、质量代理、环境通道或信息几何坐标中跨表征保持；若只在单一重参数化中出现，则不是物理不变量。 | 3.75 | 子命题提取 | 待AB |

## 声张边界

- 禁止再声称“宏观世界本体残缺已被证明”。
- 禁止把 `I=(m/m_p)^2` 作为全域精确定义。
- 禁止声称 GUP-Lindblad 链是全新；必须承认 fluctuating minimal length / deformation parameter 的先发模板。
- 当前最锋利可证伪点是 `m_max=2m_p/pi≈14 microgram`，但前提是 `q` 获得独立物理定义。

## Round 1 重算

- DGF-N4 分数暂不提升：突破潜力存在，但 INSPECTOR 给出两个阻断。
- DGF-N4d 注册为 AHA 候选，但未超过当前×1.3，不切换。

## Round 2 重算

- DGF-N4 声张保真度降至 0.7，完成度跨 30% 取 1.3。
- 新候选 DGF-N4e 未超过当前×1.3，不切换；作为 Round 3 统计化任务吸收入当前北极星。

## Round 3 重算

- DGF-N4 未被证明，且桥接未成立；当前状态转入 REVIEWER。
- DGF-N4f 是更落地候选，但仍属当前北极星的验证任务，不切换。

## Re-escalation Round 3

- DGF-N5 注册为更深候选。
- DGF-N4 强版本判定为“被推翻待强制挽救”，尚未允许降级。

## 强制挽救轮降级

- `synthesis/INSPECTOR_rescue.md` 判定：挽救失败。A 方案只把逐点 `q_i` 过拟合降为少参数风险；B 方案仍有低秩/图模型高灵活度风险；桥接 observable 未落地。
- DGF-N4 按 PI.md §2 条件B 标记为“证伪-降级”，分数降为 1.02。
- 强制分叉注册：
  - DGF-N5：跨表征不变量/不可辨识性原则，作为当前最高分北极星。
  - DGF-N6：外部 `q_DGF_mass` observable 可得性问题。
  - DGF-N7：预注册低秩/图模型在环境 null 下的反过拟合检验。

## 子命题提取后重算

- `synthesis/contradiction_deepening.md` 已把矛盾追溯到“物理机制的跨表征不变性要求 vs DGF-N4 对后验坐标/桥接假设的依赖”。
- `synthesis/subclaims_extraction.md` 提取 4 个 A vs B 子命题。
- 当前最高分并列候选为 DGF-N5、DGF-N6、DGF-N8，均为 3.75。按 SOP 深度优先与 Re-escalation 来源，DGF-N5 保持当前；DGF-N6/DGF-N8 作为 DGF-N5 Round 1 的核心子攻击面。

## DGF-N4 完成标记

结论类型：证伪-降级 / falsified-downgraded。

完成文件：

- `synthesis/PI_final_synthesis_DGF_N4.md`
- `synthesis/INSPECTOR_rescue.md`
- `synthesis/contradiction_deepening.md`
- `synthesis/subclaims_extraction.md`
- `D:\Claude\ai-reservations\knowledge_graph\anonymous-LP30_v1_20260605.json`

下一北极星：DGF-N5。
