# NSF-FCS-2 Round 2 Stop Check

**Date:** 2026-06-03

## Checklist

- Mathematical barrier: no.
- Data unavailable: no. Current finite-density ledger data are available for the required reviewer-response checks.
- A/B dual-path convergence to the same impossibility: not applicable yet for the revised Round 2 package.
- Production stagnation: no. Round 2 produced new direct-delta, off-center reverse, normalization, model-comparison, and activity-control results.
- Reviewer hard kill: no. Final reviewer recommendation was `major revision`, not rejection/kill.

## Decision

No hard stop is triggered.

This is not ready for final closure because the revised claim package has not yet been independently re-attacked by Round 2 A/B after the major-revision fixes. Per SOP, proceed to independent A/B review of the revised NSF-FCS-2 package.

## State Summary For A/B Restart

Strongest results:

- Equilibrium two-terminal second cumulant is only finite-volume FDT: `lambda_T''=2G_T`.
- The active mainline is NESS residual `R2=lambda_T''-2G_T`, not equilibrium FCS.
- Raw `R2` passes the non-circular `delta^2` test before `Qhat` normalization.
- Off-center reverse bias passes at `rho_bar=0.4,0.65`.
- `Qhat~L^{1/2}` is downgraded; current model comparison favors log drift over power-law ansatz.
- `reversible_side` activity is only an LDB sanity control; `nonLDB_site_skew` is the real non-LDB kinetic perturbation.

Known weak points:

- All numerical evidence remains small-L finite-density ledger evidence.
- `Qhat` growth mechanism is unresolved.
- The mainline is not yet a theorem and must not be phrased as established asymptotic scaling.
- Activity-control evidence is currently a sanity split, not a full scaling program.

Next instruction:

Launch independent A and B agents to attack whether the revised NSF-FCS-2 package should remain a current-mainline keep, be downgraded, or be redirected to a sharper subclaim.
