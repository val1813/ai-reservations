# 知识库

| K编号 | 状态 | 内容 | 数学对象 | 来源 |
|------|------|------|----------|------|
| K0.1 | ✅ | Noisy XX 在强噪声/热力学极限下的 correlation matrix statistics 映射到 QSSEP | Fokker-Planck / correlation matrix cumulants | R1 |
| K0.2 | ✅ | QSSEP 与其 universality class 的 transferred charge CGF 在 leading large-L 下等于 classical SSEP | cumulant generating function / large deviation function | R1 |
| K0.3 | ⚠️ | lambda(s) 与 lambda_Deph(s) 在有限 L 下不同，large-L leading order 才恢复相等 | finite-size correction / noise-realization fluctuations | R1 |
| K0.4 | ⚠️ | quasi-local hopping 是 Gauge trick 普适性论证的核心条件，长程 hopping 是自然反例入口 | locality / higher-derivative suppression | R1 |
| K1.1 | ✅ | R1 正方链只在一维、free、U(1)、unitary noise、Markovian reservoirs、quasi-local hopping、large-L diffusive scaling 下闭合 | Noisy XX -> QSSEP -> SSEP CGF | A round1 |
| K1.2 | ⚠️ | 有限尺寸 quantum correction 已有新先发 arXiv:2601.16883v1，不能作为本项目主增量 | finite-size current cumulants | A round1 |
| K1.3 | 🔥 | 长程 hopping 的强噪声投影可能给 `r_eff~|J|^2/gamma`，当 `J(r)~r^-alpha` 且 `alpha<3/2` 时进入 fractional hydrodynamics | fractional generator / long-jump exclusion | A round1 |
| K1.4 | 🔥 | shortcut/cycle 可让 counting 1-form 出现 non-exact holonomy sector，SSEP 等价可能只覆盖 exact sector | H^1(G,U(1)) / cycle holonomy | B round1 |
| K1.5 | ✅ | B 的 holonomy 方差公式经 INSPECTOR 修正为 `Var[lambda]~K0(s)^2 b1(G) Var(Phi_C)/L^2`，量纲闭合 | CGF sample variance | B inspector recheck |
| K2.1 | ✅ | 在 `gamma sum_j D[n_j]` 规范下，强退相干投影给 `r_jk=2|J_jk|^2/gamma_phi` | Zeno projection / effective exclusion rate | A round2 |
| K2.2 | ✅ | 一维 `J(r)~r^-alpha` 投影为 `r(r)~r^-2alpha`，二阶矩阈值是 `alpha=3/2`；d维为 `alpha>(d+2)/2` | second moment criterion | A round2 |
| K2.3 | ⚠️ | `lambda''(0)` 标度候选必须固定为 reservoir-current/boundary conductance，不能泛化为所有 cut-current FCS | current observable definition | Inspector A round2 |
| K2.4 | ⚠️ | shortcut holonomy 的 `O(|J_l|^2/gamma)` 阶数未证；二阶 Zeno population generator 可能吃掉 Peierls 相位，真正项或需更高阶闭合路径 | holonomy FCS | B round2 + Inspector |
| K3.1 | 🔥 | `1<alpha<3/2` 时，右 reservoir boundary conductance 标度 `G_R(L)~(2J0^2/gamma_phi) C_< L^{-(2alpha-2)}` 有 fractional Fick law 强文献支撑 | fractional Fick law / boundary conductance | A round3 |
| K3.2 | ✅ | `alpha>3/2` 为有限二阶矩普通扩散区，`G_R(L)~D_eff L^-1` 是强推断；`D_eff=(J0^2/gamma_phi)sum_r r^{2-2alpha}` | ordinary diffusion / finite second moment | A round3 |
| K3.3 | ⚠️ | `alpha=3/2` 的 `(log L)/L` 只能保留为临界候选/强推断，不得标为已证 | marginal second moment | Inspector A round3 |
| K3.4 | ⚠️ | `lambda_R''(0)` 与 boundary conductance 同标度是 fluctuation/FCS 层面的额外假设，需 tilted generator 或 Green-Kubo 验证 | reservoir-current FCS | A round3 + Inspector |
| K3.5 | ✅ | holonomy 副线的正确地位是 residual test：`theta=0` 验证主线，full tilted Lindblad 检查扣除 `D_eff` 后的 `Q(s,theta)`；无 residual 则击毙 | nuisance-orthogonal residual | B round3 |
| FCS1.1 | ✅ | 右 reservoir-current tilted generator 只 tilt 右端注入/抽出事件，SCGF 为 `principal_eigenvalue(L_s^R)` | tilted generator / reservoir current | NSF-FCS A round1 |
| FCS1.2 | ✅ | `lambda_R''(0)=<a_R>_pi+2<b_R-J_R,phi>_pi`，`L phi=-(b_R-J_R)`；这是有限体积 Poisson/resolvent 问题 | Poisson equation / Green-Kubo | NSF-FCS A+B round1 |
| FCS1.3 | ⚠️ | `lambda_R''(0)~G_R(L)` 尚非定理；缺口是 resolvent / boundary mobility 的同阶控制 | H_-1 norm / boundary observable | NSF-FCS synthesis |
| FCS1.4 | 🔎 | 可计算诊断为 `S_R(L)=lambda_R''(0)/G_R(L)`，并分别跟踪 `A_R(L)` 与 `K_R(L)` | activity-Green decomposition | NSF-FCS B round1 |
| FCS1.5 | ⚠️ | buffer reservoir 只能作为诊断性构造；必须先盲校准同标度 `G_R`，再计算 FCS，不能作为已证模型内反例 | diagnostic countermodel | Inspector B recheck |
| FCS2.1 | 🔥 | reservoir jump-current FCS 二阶项不是单纯正 `H_-1`；完整公式需 `lambda''=<a>+2<B phi>=<a>+2<h,phi>+2<C>` | tilted perturbation / martingale cross term | NSF-FCS A round2 recheck |
| FCS2.2 | ✅ | `H_-1` 只能作为 corrector/trace 控制工具，不能直接承载完整 jump-current variance 或同标度结论 | H_-1 norm limitation | Inspector A round2 recheck |
| FCS2.3 | 🔎 | 证明 `lambda_R''~G_R` 需要同时控制 boundary trace 和 activity/cross-term cancellation | cancellation mechanism | PI NSF-FCS round2 |
| FCS2.4 | 🔎 | 数值算法必须输出 `<a>`、`2<h,phi>`、`2<C>` 或直接由 tilted generator 二阶差分验证总 `lambda''` | numerical decomposition | PI NSF-FCS round2 |
## NSF-FCS Round 3 新增记录

| K编号 | 状态 | 内容 | 数学对象 | 来源 |
|------|------|------|----------|------|
| FCS3.1 | 🔥 | NSF-FCS-1 的待证对象应改写为 corrector residual/capacity bound：`D_R(L)=<sum_R c(q_R+Delta phi)^2>_pi=O(G_R(L))`，而不是单纯 `H_-1` 小 | carré-du-champ residual / fractional capacity | NSF-FCS A round3 + PI |
| FCS3.2 | ✅ | fast open SSEP 给出机制对照：`<a_R>=O(1)` 但 `lambda_R''=O(L^-1)`，因此 activity 必须被 Poisson corrector + martingale cross term 强抵消 | open SSEP current FCS | NSF-FCS A round3 + Inspector |
| FCS3.3 | ⚠️ | 未缩放 long-jump reservoir 若 `r_R(x)~x^{-alpha}` 且 `alpha>1`，则 `A_R(L)=sum_x r_R(x)=O(1)`；若模型含额外 `L` 缩放，则需改判为 slow-boundary 型 | reservoir activity scaling | Inspector A round3 |
| FCS3.4 | 🔥 | 数值台账必须区分 `C_explicit` 与 `C_from_tilted_residual`；后者不能再作为 decomposition balance 的独立验证 | tilted residual / cross term audit | NSF-FCS B round3 + Inspector |
| FCS3.5 | 🔍 | 下一步最高价值测试是单粒子 long-jump reservoir 的 Poisson/tilted 台账：若单粒子层面 `D_R(L)` 不同标度于 `G_R(L)`，则 NSF-FCS-1 强命题应 kill | single-particle falsification test | PI NSF-FCS round3 |
## NSF-FCS Round 4 新增记录

| K编号 | 状态 | 内容 | 数学对象 | 来源 |
|------|------|------|----------|------|
| FCS4.1 | ✅ | bare right-reservoir exchange current 在单粒子闭合/右库节点模型中是 occupation coboundary：`Q_R(t)=psi(X_0)-psi(X_t)`，故长期 `lambda_R''(0)=0` | additive functional coboundary | NSF-FCS A round4 + Inspector |
| FCS4.2 | 🔥 | NSF-FCS-1 原始 bare-exchange 强命题应 kill；可保留的对象必须是 two-terminal transported current，而不是 bare reservoir exchange | observable redefinition | PI NSF-FCS round4 |
| FCS4.3 | ⚠️ | `D_R(L)` 是 `Cap(R,0)=E(u,u)` 的 reservoir-edge 非负部分，方向上 `D_R<=Cap`；`D_R~Cap` 是未证 conditional lemma，不能由 Dirichlet 原理自动推出 | capacity / boundary residual | Inspector A round4 |
| FCS4.4 | ✅ | B 的 transport ledger 修正版通过：open sector `Omega={e,1..L}`、source/sink reservoirs、two-terminal current、`G_R=dJ_T/dF`、`S_T=lambda_T''/G_R` 自洽 | two-terminal transport ledger | B round4 recheck |
| FCS4.5 | 🔍 | bare/coboundary observable 必须硬门控为 `INVALID_BARE_EXCHANGE_NOT_TRANSPORT`，不得进入 keep/kill；科学判据只能依赖 valid transport ledger 的 `lambda_T''/G_R` | ledger quality gate | PI NSF-FCS round4 |
## NSF-FCS Transport Ledger 新增记录

| K编号 | 状态 | 内容 | 数学对象 | 来源 |
|------|------|------|----------|------|
| FCS5.1 | ✅ | 已实现 channel-resolved two-terminal single-particle transport ledger：reservoir 作为 source/sink rates，observable 为 left throughput / transport current | finite Markov generator / tilted spectrum | `scripts/nsf_fcs_transport_ledger.py` |
| FCS5.2 | ✅ | 正式台账 `L=16,24,32,48,64`、`alpha=0.5,1.0,1.5` 全部 valid；`lambda_T''` 与 `G_R` log-slope 完全一致，`S_T≈2` | `S_T=lambda_T''/G_R` | `current/plan/nsf_fcs_transport_ledger.csv` |
| FCS5.3 | ✅ | bare sanity check 被硬门控为 `INVALID_BARE_EXCHANGE_NOT_TRANSPORT`，`S_R=NA`，未进入 keep/kill | observable quality gate | `current/plan/nsf_fcs_transport_ledger_bare_check.csv` |
| FCS5.4 | 🔍 | transport-FCS 改写版通过 minimal falsifier，但仍不是 many-particle theorem；下一阶段应证明 `D_R~Cap` 或扩展到 finite-density exclusion ledger | minimal falsifier / next stage | PI ledger result |
## NSF-FCS Exclusion Ledger 新增记录

| K编号 | 状态 | 内容 | 数学对象 | 来源 |
|------|------|------|----------|------|
| FCS6.1 | ✅ | 已将 transport ledger 扩展到 finite-density exclusion sector，状态空间为 bitmask `2^L`，bulk exclusion jumps 与 left/right source-sink reservoir channel-resolved | finite-density exclusion generator | `scripts/nsf_fcs_transport_ledger.py --sector exclusion` |
| FCS6.2 | ✅ | `L=4..8`、`alpha=0.5,1.0,1.5` 的 exclusion ledger 全部 valid；`lambda_T''` 与 `G_R` log-slope 完全一致 | finite-density FCS / conductance | `current/plan/nsf_fcs_exclusion_ledger.csv` |
| FCS6.3 | 🔥 | single-particle 与 finite-density ledger 均给出 `S_T≈2`，提示该规范下可能存在有限体积 Einstein/FDT 恒等式 `lambda_T''(0)=2G_R` | fluctuation-dissipation relation | PI exclusion ledger result |
| FCS6.4 | 🔍 | 下一步应证明 `lambda_T''=2G_R` 是否为 equilibrium two-terminal symmetric gauge 的有限体积恒等式；若成立，真正风险转向非平衡密度、非对称 reservoir 或高阶 cumulants | FDT boundary | PI exclusion ledger result |
## NSF-FCS Round 5 新增记录

| K编号 | 状态 | 内容 | 数学对象 | 来源 |
|------|------|------|----------|------|
| FCS7.1 | ✅ | 在 finite-state Markov jump process、local detailed balance、equilibrium two-terminal entropy affinity 下，`lambda_T''(0)=2G_T` 是有限体积 FDT/Einstein-Helfand 恒等式 | GC symmetry / FDT | A round5 + Inspector |
| FCS7.2 | ⚠️ | 当前脚本正向规范为 `A_ledger=F=eta_R-eta_L`，左端 out-in current 为正；`logit(rho_L)-logit(rho_R)` 写法与该规范差整体号向 | affinity sign convention | Inspector A round5 |
| FCS7.3 | ✅ | equilibrium 二阶 DC transport-FCS 分支应降级为 FDT corollary；`lambda_T''` 自动继承 `G_T(L)` 标度，系数为 2 | second cumulant / conductance | PI round5 |
| FCS7.4 | 🔥 | 新最高价值方向转为 `FDT_residual_R2=lambda_T''-2G_T` 在 NESS、non-LDB、activity forcing、finite-frequency 或 high cumulants 中是否携带 long-jump 标度 | FDT residual / nonlinear response | B round5 + PI |
| FCS7.5 | ⚠️ | finite-frequency resolvent 必须显式投影到 zero-mean subspace 并说明 row-generator / backward-generator convention；不能直接把 DC Poisson 公式机械替换 | dynamic noise convention | Inspector B round5 |
## NSF-FCS Tier 1 NESS 新增记录

| K编号 | 状态 | 内容 | 数学对象 | 来源 |
|------|------|------|----------|------|
| FCS8.1 | ✅ | affinity split sanity 通过：`eta_R=aF, eta_L=-(1-a)F` 不破坏 equilibrium `S_T≈2`，说明 total entropy affinity 规范自洽 | affinity split / FDT sanity | `current/plan/nsf_fcs_tier0_split_*.csv` |
| FCS8.2 | 🔥 | NESS density bias `rho_left=0.4, rho_right=0.6` 打开 FDT residual：`S_T` 偏离 2，`R2=lambda_T''-2G_T` 非零且有稳定标度 | nonequilibrium FDT residual | `current/plan/nsf_fcs_tier1_ness_exclusion.csv` |
| FCS8.3 | 🔥 | 新更高价值议题：`NSF-FCS-2`，研究 long-jump open exclusion 在 NESS 背景下的 `R2(L)` 标度，而不是 equilibrium 二阶 FCS | NESS residual scaling | PI tier1 NESS result |
## NSF-FCS-2 NESS bias scan 新增记录

| K编号 | 状态 | 内容 | 数学对象 | 来源 |
|------|------|------|----------|------|
| FCS9.1 | ✅ | 三组 NESS density-bias ledger 全部 valid：`rho_left/rho_right=0.45/0.55,0.40/0.60,0.35/0.65`，`L=4..8`，`alpha=0.5,1.0,1.5`，共 45 行 | finite-density exclusion tilted generator | `current/plan/nsf_fcs_tier1_ness_0p*_0p*.csv` |
| FCS9.2 | 🔥 | `R2=lambda_T''-2G_T` 对 density bias 幅度呈近乎精确二次增长；固定 `L=8` 时三个 alpha 的 delta 斜率约为 `2.000030,2.000000,1.999998` | near-equilibrium NESS residual | PI NESS bias scan |
| FCS9.3 | 🔥 | `R2` 的 `L` 斜率按 alpha 稳定分组：`alpha=0.5 -> ~1.009`，`alpha=1.0 -> ~0.621`，`alpha=1.5 -> ~0.279`，说明 residual 不只是 affinity split 或数值噪声 | reservoir-tail scaling diagnostic | PI NESS bias scan |
| FCS9.4 | ✅ | NSF-FCS-2 正式切为当前北极星；NSF-FCS-1 equilibrium second cumulant 分支保留为 FDT corollary，不再作为主攻问题 | priority queue switch | project/北极星队列.md |
| FCS9.5 | ✅ | 反向偏置 `rho_left/rho_right=0.6/0.4` 与正向 `0.4/0.6` 对照通过；`max |Delta S_T|=6.03e-8`，`max |Delta R2|=8.46e-9`，说明二阶 residual 对 bias reversal 为偶函数到数值精度 | reversal sanity / sign convention | `current/plan/nsf_fcs_tier1_ness_reverse_0p60_0p40.csv` |

## NSF-FCS-2 Round 1 新增记录

| K编号 | 状态 | 内容 | 数学对象 | 来源 |
|------|------|------|----------|------|
| FCS10.1 | ✅ | `R2(delta)` 的首个非零项为 `delta^2` 不是无条件定理；它依赖 finite-L 解析性、equilibrium FDT、左右反射偶性、two-terminal 二阶 observable 与 local detailed balance | near-equilibrium expansion | A round1 + Inspector |
| FCS10.2 | 🔥 | 更干净的诊断量是 `Qhat=R2/(delta^2 G_T)=(S_T-2)/delta^2`；三组 centered-bias ledger 给出 `Qhat` 斜率约 `0.47..0.54`，几乎与 density bias 无关 | normalized NESS residual | PI round1 synthesis |
| FCS10.3 | ⚠️ | `Qhat~L^{1/2}`、`R2~delta^2 G_T Q_L`、`3/2-alpha` 解释目前只是 ansatz，不得写成定理 | Green/McLennan ansatz | Inspector A round1 |
| FCS10.4 | ✅ | B 的 falsifier 修正后通过：`B2` 等 Taylor 系数单位为 `1/time`，`tau_J=R2/J0^2` 是时间尺度，`Q_JG=R2*G_T/J0^2` 才是无量纲 current-normalized 诊断 | dimensional audit / falsifier design | Inspector B recheck |
| FCS10.5 | 🔥 | activity forcing 必须区分 `reversible_side` 与 `nonLDB_site_skew`；同侧 in/out 同乘 activity 若保持 LDB，应作为 `R2≈0` sanity control，不是 kinetic residual 证据 | non-LDB forcing design | B round1 recheck |
| FCS10.6 | 🔥 | off-center density falsifier 通过：`rho_bar=0.4,0.5,0.65` 且 `delta=0.2` 时，`Qhat=R2/(delta^2 G_T)` 的 `L=4..7` 斜率在每个 alpha 下几乎不变；幅度随 `rho_bar` 增大但指数不变 | particle-hole artifact test | offcenter ledgers + PI synthesis |
